# 当前实验状态

最后更新：2026-09-11（Asia/Shanghai）

当前研究阶段：**explore**

当前实验阶段：**E0 — 数据与构念就绪 / 实验前准备**

当前运行：**无**

## 当前唯一研究定位

探索明确 AI 工具条件下的纵向协作编程能力，后续 AI 辅助新任务表现为主，独立编程/代码理解为辅助。创新尚未确定，当前先阅读最近邻并验证可测性；保留三条假设：

1. 条件化的纵向协作能力预测；
2. 协作行为的可靠、可迁移测量；
3. AI 工具变化下的能力评价稳定性。

当前不承诺新的 KT 架构，不把“加入聊天特征”本身当作创新，也不把模拟学生或系统掌握度当作真实能力真值。

## 当前权威文件与优先级

发生冲突时按以下顺序解释：

1. `research/state.json` 中 `current_archive` 和 `current_authority` 指向的当前讨论归档
2. `research/state.json` 中 `current_review` 指向的当前文献地图
3. 本文件、`research/state.json`、`experiments/registry.csv`
4. `research/decision-log.md` 的最新追加条目
5. 2026-08-24 落地方案和 2026-09-07 方向评估，仅作历史追溯

日期文档是不可变快照。新定位必须创建新的日期文件，再更新 `state.json` 的当前入口；不得为了“更新当前内容”直接改写旧日期文件。

## 已完成的里程碑

- 2026-08-24：完成原始研究计划的技术路线、数据集和实验编排建议；该版本已标记为历史方案。
- 2026-08-24：完成本地数据聚合审计；原始学生数据不进入 Git。
- 2026-09-07：完成最近邻与公开数据元数据复核（`M20260907-001`）；StudyChat/SQKT 的直接重合使简单聊天特征增量主张失效。
- 2026-09-09：完成讨论归档与定向文献刷新（`M20260909-001`）；研究定位回到 `explore / E0`，无模型结果、无学生数据处理。
- 2026-09-11：修正状态阶段语义、恢复 2026-09-07 manifest 对应原始字节，并固定日期快照交接规则；无研究内容变化。

## 当前候选方向与门槛

| 方向 | 当前角色 | 先决门槛 |
|---|---|---|
| A：条件化纵向协作能力预测 | 优先阅读 | 真实 learner-task 序列、明确 AI 工具条件、独立未来 AI 辅助结果 |
| B：协作行为可靠测量 | 并行备用 | 合法可用轨迹、双人盲标量规、外部结果或跨任务留出 |
| C：跨工具评价稳定性 | 暂缓扩展 | 同一学生在等价任务上的工具条件重叠与锚定任务 |

## 下一批工作登记

| ID | 状态 | 优先级 | 目标 |
|---|---|---|---|
| `E20260826-001-local-linkage-audit` | planned | next | 审计真实数据的学生—任务—消息—代码状态联结、工具条件、标签来源和泄漏风险 |
| `E20260826-002-public-pkt-baseline` | planned | deferred | 仅在阅读/新颖性门槛通过，或为特定可否证比较确实需要时，再运行 CodeWorkout/TIKTOC 基线 |

详细路径、配置和 manifest 见 `experiments/registry.csv`。`planned` 不代表已经运行；没有结果文件和 notes 就不能写成完成。

## 已确定的研究规则

- 先证明观测增量性，再比较行为/语义表示，最后才判断是否需要新 KT 架构。
- 使用 learner-task episode 作为分析单位，保留消息、代码状态、测试和工具条件 provenance。
- 回顾性数据只支持预测与关联；历史自选择 AI 使用不能解释为因果效应。
- 指标按结果类型选择：二元/概率结果用 Log Loss、Brier、AUROC、AUPRC 和校准；连续成绩用 MAE、RMSE、留出 R²。
- 统计单位以学生为主，使用学生级 bootstrap 或层级方法；禁止相邻提交随机拆分到训练和测试。
- 本地 L1–L4 历史事件只有 93 次、涉及 12 人，不能支持剂量反应或因果结论。
- Git 只同步可共享文档、状态、去标识摘要和小型结果；私有数据、数据库导出、凭据、完整日志和大文件不进入 Git。

## 当前阻塞项

- 本地任务外键、AI 消息级导出、AI 建议到代码修改的 provenance 不完整。
- KC 缺失且历史清洗存在过度映射风险；有效样本量尚未由完整联结/效标审计确定。
- StudyChat 文件访问受限，尚未下载和冻结；TutorTrace 官网与论文样本口径及许可仍需核实。
- 尚无训练入口、依赖锁、自动化测试和公共数据下载 manifest。

## 下一步

1. 完成一至两天最近邻阅读，形成 A/B/C 各自的输入—目标—真值—最强基线—否证条件表。
2. 优先核验 StudyChat 的跨作业对话—代码—成绩联结和 AI 工具条件。
3. 在合法授权前提下设计 `E20260826-001` 的去标识 schema、数据 manifest 和链接覆盖审计。
4. 只有当前研究状态明确放行后，才把 deferred 的公共 PKT 基线提升为 next。

## 跨设备恢复规则

新设备开始工作前固定执行：

```powershell
git status --short --branch
git pull --ff-only
```

然后按 `AGENTS.md` 的必读顺序读取 `state.json` 当前入口指向的日期文档。任何研究定位变化都必须创建新的日期文档，再同步 `research/state.json`、本文件、registry 和 `research/decision-log.md`；旧日期快照不得改写。Git 冲突要语义合并，不能用一侧覆盖状态。

完整日志、大型结果和私有数据通过 Git 外的合规渠道管理，并用 data manifest/hash 确认版本。
