# M20260909-001 discussion and literature refresh

Portable discussion archive, nearest-work reading map and current-target reconciliation completed. No model experiment or private-data access.

Baseline commit: a23f0b8; documentation milestone, not a reproducible model benchmark. SHA-256 and logical repo URIs: public-source-review-20260909.json. Retain curated documents in Git; retrieval caches disposable.

Coverage: bounded refresh, not systematic saturation. Some sources marked abstract-only. TutorTrace license/use unresolved. Historical 2026-09-07 material retained. Reading agenda and prototype proposal not executed.

Historical integrity note: the 2026-09-07 manifest includes the then-current mutable dataset-evaluation-matrix.csv. That matrix was updated on 2026-09-09; its old hash no longer matches the current matrix. The prior matrix snapshot was not separately frozen in Git, so that historical byte version cannot be claimed recoverable. Keep the old manifest unchanged as provenance; use the 2026-09-09 manifest for the current matrix. Dated immutable reports remain available. Future manifests should reference dated snapshots for mutable artifacts.

Validation: workspace validator reported 0 errors / 0 warnings (3105 corpus records, 54 local notes). Registry artifact paths exist; all five current-manifest SHA-256 values match staged Git blobs. Staged paths contain no private-data directories or temporary scripts; credential-pattern scan returned no matches. Legacy 2026-09-07 Markdown line breaks and blank EOF lines were preserved, so diff whitespace check reports those historical formatting warnings. Old raw-byte hashes may also depend on original CRLF/LF checkout; the new manifest was verified against canonical Git bytes for cross-device use.
